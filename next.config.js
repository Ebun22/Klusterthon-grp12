/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        pathname: '**.png'
      },
      reactStrictMode: true,
      concurrentFeatures: true,
}

module.exports = nextConfig
